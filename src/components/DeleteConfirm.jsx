import { Modal,Text } from '@mui/material'
import React from 'react'

function DeleteConfirm({open,setOpen}) {
    const handleClose = () => setOpen(false);
  return (
    <Modal>
        <Box>
        </Box>

    </Modal>  
  )
}

export default DeleteConfirm